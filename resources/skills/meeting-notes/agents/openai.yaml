interface:
  display_name: "Meeting Notes"
  short_description: "Summarize meetings into decisions and action items"
  brand_color: "#0891B2"
  default_prompt: "Use $meeting-notes to turn this transcript into decisions, action items, and follow-ups."
policy:
  allow_implicit_invocation: true
