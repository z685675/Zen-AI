## 修复

- 修复 `v1.1.53` 在导入备份并自动重启后，启动阶段可能卡死、窗口无法打开的问题。
- 优化恢复后的内部路径迁移性能：对大型会话消息先做快速候选判断，并使用更具体的路径匹配，避免在数 MB 级消息内容上触发高 CPU 正则扫描。
- 内部路径迁移增加完成标记；没有新的恢复任务时，不再每次启动都重复扫描 `agents.db` 和笔记文件。

## 说明

- 已经在 `v1.1.53` 遇到无法启动的用户，安装 `v1.1.54` 后再次启动即可继续完成迁移。
- 本次仍只迁移 Zen AI / Cherry Studio 应用内部 `Data` 目录路径，不会改写用户外部文件夹、微信临时目录、下载目录等路径。

## 验证

- 使用真实恢复后的 `agents.db` 做只读迁移性能验证，159 条消息内容约 10MB，迁移扫描耗时约 61ms。
- 通过内部路径迁移单元测试。
- 通过备份恢复启动逻辑单元测试。
