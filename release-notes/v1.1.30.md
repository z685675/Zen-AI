## Zen AI v1.1.30

本次版本修复 macOS 发布构建在未配置 macOS 签名证书时失败的问题。

### 更新内容

- macOS 构建仅在配置了 `MAC_CSC_LINK` 时启用签名证书环境变量。
- 未配置 macOS 签名证书时，显式关闭证书自动发现，恢复未签名 macOS 安装包构建。
- 继续保留自动更新链路修复：macOS 安装触发、旧待安装包追到最新版。

